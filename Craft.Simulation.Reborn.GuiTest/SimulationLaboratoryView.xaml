<UserControl x:Class="Craft.Simulation.Reborn.GuiTest.SimulationLaboratoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Craft.Simulation.Reborn.GuiTest"
             xmlns:reborn="clr-namespace:Craft.UIElements.Geometry2D.Reborn;assembly=Craft.UIElements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <Button Content="{Binding StartOrResumeButtonText}"
                    Width="60" Height="20" Margin="10"
                    HorizontalAlignment="Left" VerticalAlignment="Top"
                    Command="{Binding StartOrResumeAnimationCommand}"/>
            <Button Content="Pause" Width="60" Height="20" Margin="10, 0, 10, 10"
                    HorizontalAlignment="Left" VerticalAlignment="Top"
                    Command="{Binding PauseAnimationCommand}"/>
        </StackPanel>

        <local:SceneListView Grid.Row="1" Grid.Column="0" DataContext="{Binding SceneListViewModel}"/>

        <reborn:GeometryView Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                             DataContext="{Binding}"
                             FrameHandler="{Binding}"/>
    </Grid>
</UserControl>
